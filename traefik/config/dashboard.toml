[http.middlewares.dash-auth.basicAuth]
  users = [
    "test:$apr1$lPMwVjPa$HD4BusRHAV/ARVVnWpxUG."
  ]

[http.routers.api]
  rule = "Host(`poding.net`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))"
  entrypoints = ["http"]
  service = "api@internal"
  middlewares = ["dash-auth"]

[http.routers.ping]
  rule = "Host(`poding.net`) && PathPrefix(`/ping`)"
  entrypoints = ["http"]
  service = "ping@internal"
  middlewares = ["dash-auth"]

